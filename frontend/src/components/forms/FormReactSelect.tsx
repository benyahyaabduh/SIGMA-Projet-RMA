import React, { FC } from "react";
import { Box } from "@mui/material";

interface FormReactSelectProps {
  name: string;
  label: any;
}

const FormReactSelect: FC<FormReactSelectProps> = () => {
  return <Box />;
};

export default FormReactSelect;
